# 嵌入式单元测试用例（battery + led 模块）

| 项目 | 内容 |
|------|------|
| 文档名称 | 嵌入式单元测试用例（battery 电压采集 + led 指示灯） |
| 版本 | V2.0 |
| 编制日期 | 2026-09-02（V1.0）/ 2026-09-05（V2.0 新增 led 模块） |
| 对应测试方案 | 测试方案.md |
| 用例总数 | 19（battery 11 + led 8） |
| 执行结果 | 19 通过 / 0 失败 / 0 阻塞 |

---

## 1. 用例概览

### 1.1 按测试方法统计

| 测试方法 | 用例数 | 用例编号 |
|----------|--------|----------|
| 等价类划分 | 5 | TC-001, TC-002, TC-012, TC-013, TC-014 |
| 边界值分析 | 7 | TC-003, TC-004, TC-005, TC-015, TC-016, TC-017, TC-018 |
| 错误推测 | 2 | TC-006, TC-007 |
| 接口交互测试 | 3 | TC-008, TC-009, TC-010 |
| 容错测试 | 1 | TC-011 |
| 状态切换测试 | 1 | TC-019 |
| **合计** | **19** | |

### 1.2 按被测模块统计

| 被测模块 | 用例数 | 用例编号 |
|----------|--------|----------|
| battery（电池电压采集） | 11 | TC-001 ~ TC-011 |
| led（LED 指示灯控制） | 8 | TC-012 ~ TC-019 |
| **合计** | **19** | |

### 1.3 换算公式参考

本模块换算公式（用于计算预期值）：

```
measured_mv = adc_raw × 3300 / 4095      （ADC 引脚测得电压，整数除法）
battery_mv  = measured_mv × 3              （还原电池电压）
若 battery_mv > 8400，则钳位为 8400
若 adc_raw > 4095（非法输入），则返回 0
```

关键计算示例：

| adc_raw | measured_mv | battery_mv（钳位前） | 最终结果 |
|---------|-------------|---------------------|----------|
| 0 | 0 | 0 | 0 |
| 1000 | 805 | 2415 | 2415 |
| 2048 | 1650 | 4950 | 4950 |
| 3475 | 2800 | 8400 | 8400（不触发钳位） |
| 3476 | 2801 | 8403 | 8400（触发钳位） |
| 4000 | 3226 | 9678 | 8400（触发钳位） |
| 4095 | 3300 | 9900 | 8400（触发钳位） |
| 5000 | — | — | 0（非法输入） |
| 0xFFFF | — | — | 0（非法输入） |

---

## 2. 测试用例详情

### TC-001：正常 ADC 值换算（等价类-有效等价类）

| 项目 | 内容 |
|------|------|
| 用例编号 | TC-001 |
| 用例名称 | test_adc_to_mv_normal |
| 测试方法 | 等价类划分（有效等价类） |
| 被测接口 | `adc_to_mv()` |
| 前置条件 | 桩状态已初始化（setUp 中 mock_hal_adc_Init） |
| 输入 | `adc_raw = 2048`（12 位 ADC 中间值） |
| 操作步骤 | 1. 调用 `adc_to_mv(2048)`<br>2. 用 `TEST_ASSERT_EQUAL_UINT16` 校验返回值 |
| 预期结果 | 返回 `4950`（2048×3300/4095=1650, ×3=4950，低于 8400 上限，不触发钳位） |
| 实际结果 | 4950 |
| 状态 | ✅ 通过 |
| 备注 | 验证正常输入下换算公式正确，且钳位逻辑不会"误杀"正常值 |

---

### TC-002：低于上限的正常值不被误钳位（等价类-有效等价类）

| 项目 | 内容 |
|------|------|
| 用例编号 | TC-002 |
| 用例名称 | test_adc_to_mv_below_max |
| 测试方法 | 等价类划分（有效等价类） |
| 被测接口 | `adc_to_mv()` |
| 前置条件 | 桩状态已初始化 |
| 输入 | `adc_raw = 1000` |
| 操作步骤 | 1. 调用 `adc_to_mv(1000)`<br>2. 校验返回值 |
| 预期结果 | 返回 `2415`（1000×3300/4095=805, ×3=2415，远低于 8400 上限） |
| 实际结果 | 2415 |
| 状态 | ✅ 通过 |
| 备注 | 验证钳位逻辑仅在超限时触发，不会影响正常范围内的值 |

---

### TC-003：ADC 最小值边界（边界值-下边界）

| 项目 | 内容 |
|------|------|
| 用例编号 | TC-003 |
| 用例名称 | test_adc_to_mv_zero |
| 测试方法 | 边界值分析（下边界） |
| 被测接口 | `adc_to_mv()` |
| 前置条件 | 桩状态已初始化 |
| 输入 | `adc_raw = 0`（ADC 最小有效值） |
| 操作步骤 | 1. 调用 `adc_to_mv(0)`<br>2. 校验返回值 |
| 预期结果 | 返回 `0` |
| 实际结果 | 0 |
| 状态 | ✅ 通过 |
| 备注 | 验证下边界输入处理正确；0 是 ADC 的最小有效值，不应被视为非法输入 |

---

### TC-004：ADC 最大值边界 + 上钳位（边界值-上边界）

| 项目 | 内容 |
|------|------|
| 用例编号 | TC-004 |
| 用例名称 | test_adc_to_mv_full_scale_clamp |
| 测试方法 | 边界值分析（上边界）+ 错误推测 |
| 被测接口 | `adc_to_mv()` |
| 前置条件 | 桩状态已初始化 |
| 输入 | `adc_raw = 4095`（12 位 ADC 满量程） |
| 操作步骤 | 1. 调用 `adc_to_mv(4095)`<br>2. 校验返回值 |
| 预期结果 | 返回 `8400`（4095×3300/4095=3300, ×3=9900 > 8400，触发上钳位到 8400） |
| 实际结果 | 8400 |
| 状态 | ✅ 通过 |
| 备注 | **关键用例**：验证上钳位逻辑生效。若钳位条件写反（> 写成 <），本用例会返回 9900 而非 8400，测试失败 |

---

### TC-005：钳位临界点（边界值-临界精度）

| 项目 | 内容 |
|------|------|
| 用例编号 | TC-005 |
| 用例名称 | test_adc_to_mv_clamp_critical |
| 测试方法 | 边界值分析（临界点） |
| 被测接口 | `adc_to_mv()` |
| 前置条件 | 桩状态已初始化 |
| 输入 | `adc_raw = 3475` 和 `adc_raw = 3476` |
| 操作步骤 | 1. 调用 `adc_to_mv(3475)`，校验返回值<br>2. 调用 `adc_to_mv(3476)`，校验返回值 |
| 预期结果 | - `adc_to_mv(3475)` 返回 `8400`（3475×3300/4095=2800, ×3=8400，刚好等于上限，不触发钳位）<br>- `adc_to_mv(3476)` 返回 `8400`（3476×3300/4095=2801, ×3=8403 > 8400，触发钳位到 8400） |
| 实际结果 | 8400 / 8400 |
| 状态 | ✅ 通过 |
| 备注 | 验证钳位逻辑的临界精度：相差 1 个 ADC 码值，一个自然到达边界，一个被钳位到边界，结果都正确。临界点计算：令 adc_raw×3300/4095×3 ≤ 8400，解得 adc_raw ≤ 3475.5 |

---

### TC-006：超压输入被钳位（错误推测-异常等价类）

| 项目 | 内容 |
|------|------|
| 用例编号 | TC-006 |
| 用例名称 | test_adc_to_mv_overvoltage |
| 测试方法 | 错误推测（异常等价类） |
| 被测接口 | `adc_to_mv()` |
| 前置条件 | 桩状态已初始化 |
| 输入 | `adc_raw = 4000`（非正常高值，模拟过压或采样异常） |
| 操作步骤 | 1. 调用 `adc_to_mv(4000)`<br>2. 校验返回值 |
| 预期结果 | 返回 `8400`（4000×3300/4095=3226, ×3=9678 > 8400，触发钳位） |
| 实际结果 | 8400 |
| 状态 | ✅ 通过 |
| 备注 | 对应真实场景：ADC 采样毛刺、分压电阻失效、电池过压。验证异常高输入下钳位防护生效 |

---

### TC-007：非法 ADC 输入防护（错误推测-非法输入）

| 项目 | 内容 |
|------|------|
| 用例编号 | TC-007 |
| 用例名称 | test_adc_to_mv_invalid_input |
| 测试方法 | 错误推测（非法输入） |
| 被测接口 | `adc_to_mv()` |
| 前置条件 | 桩状态已初始化 |
| 输入 | `adc_raw = 5000`（超过 12 位满量程 4095）和 `adc_raw = 0xFFFF` |
| 操作步骤 | 1. 调用 `adc_to_mv(5000)`，校验返回值<br>2. 调用 `adc_to_mv(0xFFFF)`，校验返回值 |
| 预期结果 | 均返回 `0`（BATTERY_MIN_MV）。超过硬件范围的输入被拦截，返回安全下限值 |
| 实际结果 | 0 / 0 |
| 状态 | ✅ 通过 |
| 备注 | 对应真实场景：寄存器读乱码、DMA 传输错误、未初始化内存值。若遗漏此检查，5000 会被换算为 12093mV（uint16_t 溢出），结果完全错误 |

---

### TC-008：正常读取电池电压（接口交互-调用次数校验）

| 项目 | 内容 |
|------|------|
| 用例编号 | TC-008 |
| 用例名称 | test_read_battery_mv_success |
| 测试方法 | 接口交互测试（CMock 桩校验） |
| 被测接口 | `read_battery_mv()` |
| 前置条件 | 桩状态已初始化 |
| 输入 | 预设 `HAL_ADC_GetValue()` 返回 `2048` |
| 操作步骤 | 1. 调用 `HAL_ADC_GetValue_ExpectAndReturn(2048)` 预设一次调用<br>2. 调用 `read_battery_mv()`<br>3. 校验返回值<br>4. tearDown 中 `mock_hal_adc_Verify()` 校验调用次数 |
| 预期结果 | - `read_battery_mv()` 返回 `4950`<br>- HAL 被调用恰好 1 次（Verify 通过） |
| 实际结果 | 4950，调用 1 次 |
| 状态 | ✅ 通过 |
| 备注 | 验证 read_battery_mv() 正确调用 HAL 接口（次数=1），且对返回值的换算处理正确 |

---

### TC-009：连续读取两次（接口交互-调用顺序与次数校验）

| 项目 | 内容 |
|------|------|
| 用例编号 | TC-009 |
| 用例名称 | test_read_battery_mv_multi_calls |
| 测试方法 | 接口交互测试（调用顺序 + 次数校验） |
| 被测接口 | `read_battery_mv()` |
| 前置条件 | 桩状态已初始化 |
| 输入 | 预设第一次 HAL 返回 `1000`，第二次返回 `2048` |
| 操作步骤 | 1. `HAL_ADC_GetValue_ExpectAndReturn(1000)`<br>2. `HAL_ADC_GetValue_ExpectAndReturn(2048)`<br>3. 第一次调用 `read_battery_mv()`，校验返回值<br>4. 第二次调用 `read_battery_mv()`，校验返回值<br>5. tearDown 中 Verify 校验调用次数=2 |
| 预期结果 | - 第一次返回 `2415`（1000 换算）<br>- 第二次返回 `4950`（2048 换算）<br>- HAL 被调用恰好 2 次，且按预设顺序返回 |
| 实际结果 | 2415 / 4950，调用 2 次 |
| 状态 | ✅ 通过 |
| 备注 | 验证多次读取时 HAL 调用顺序和次数正确，每次返回值按预设顺序对应 |

---

### TC-010：Ignore 模式固定返回（接口交互-Ignore 插件）

| 项目 | 内容 |
|------|------|
| 用例编号 | TC-010 |
| 用例名称 | test_read_battery_mv_ignore_mode |
| 测试方法 | 接口交互测试（Ignore 插件） |
| 被测接口 | `read_battery_mv()` |
| 前置条件 | 桩状态已初始化 |
| 输入 | `HAL_ADC_GetValue_IgnoreAndReturn(2048)`（Ignore 模式，固定返回 2048） |
| 操作步骤 | 1. 设置 Ignore 模式<br>2. 连续调用 `read_battery_mv()` 3 次<br>3. 每次校验返回值<br>4. tearDown 中 Verify 跳过次数校验（Ignore 模式） |
| 预期结果 | 3 次调用均返回 `4950`（2048 换算结果），不校验 HAL 调用次数 |
| 实际结果 | 4950 / 4950 / 4950 |
| 状态 | ✅ 通过 |
| 备注 | 验证 Ignore 插件工作方式：适用于"不关心底层调用次数，只关注上层业务逻辑"的测试场景 |

---

### TC-011：HAL 返回错误码时的容错（容错测试）

| 项目 | 内容 |
|------|------|
| 用例编号 | TC-011 |
| 用例名称 | test_read_battery_mv_hal_error |
| 测试方法 | 容错测试（错误推测） |
| 被测接口 | `read_battery_mv()` |
| 前置条件 | 桩状态已初始化 |
| 输入 | 预设 `HAL_ADC_GetValue()` 返回 `HAL_ADC_ERROR`（0xFFFF） |
| 操作步骤 | 1. `HAL_ADC_GetValue_ExpectAndReturn(HAL_ADC_ERROR)`<br>2. 调用 `read_battery_mv()`<br>3. 校验返回值<br>4. tearDown 中 Verify 校验调用次数=1 |
| 预期结果 | 返回 `BATTERY_READ_ERROR`（-1），而不是将 0xFFFF 当作合法 ADC 值换算 |
| 实际结果 | -1 |
| 状态 | ✅ 通过 |
| 备注 | **关键容错用例**：若 battery.c 中遗漏 HAL_ADC_ERROR 检查，0xFFFF 会被当作 ADC 值换算（65535×3300/4095=52800mV，uint16_t 溢出），结果完全错误。验证硬件读取失败时函数返回明确错误码，上层可据此采取降级策略 |

---

## 2.2 LED 指示灯模块（led.c）—— TC-012 ~ TC-019

**被测函数**：`led_set_by_voltage(uint16_t battery_mv)`、`led_get_state(void)`

**电压区间划分**：
- 低压：`battery_mv < 6000` → 红灯亮（RED_PIN=HIGH, GREEN_PIN=LOW），状态 RED
- 正常：`6000 ≤ battery_mv ≤ 8400` → 绿灯亮（RED_PIN=LOW, GREEN_PIN=HIGH），状态 GREEN
- 过压：`battery_mv > 8400` → 红灯亮（RED_PIN=HIGH, GREEN_PIN=LOW），状态 RED

**桩依赖**：`HAL_GPIO_SetPin(gpio_pin_t pin, gpio_level_t level)`（CMock 生成 mock_hal_gpio）

### TC-012：低压区间 —— 红灯亮

| 项目 | 内容 |
|------|------|
| 用例编号 | TC-012 |
| 用例名称 | test_led_low_voltage |
| 测试方法 | 等价类划分（低压有效等价类） |
| 前置条件 | 桩状态已初始化 |
| 输入 | `battery_mv = 5000`（< 6000，低压区间） |
| 操作步骤 | 1. `HAL_GPIO_SetPin_Expect(LED_RED, HIGH)`<br>2. `HAL_GPIO_SetPin_Expect(LED_GREEN, LOW)`<br>3. 调用 `led_set_by_voltage(5000)`<br>4. 校验 `led_get_state()` 返回值 |
| 预期结果 | 红灯 HIGH、绿灯 LOW；状态 = LED_STATE_RED |
| 实际结果 | 状态 = RED |
| 状态 | ✅ 通过 |
| 备注 | 验证低压区间 GPIO 操作和状态更新正确 |

### TC-013：正常区间 —— 绿灯亮

| 项目 | 内容 |
|------|------|
| 用例编号 | TC-013 |
| 用例名称 | test_led_normal_voltage |
| 测试方法 | 等价类划分（正常有效等价类） |
| 前置条件 | 桩状态已初始化 |
| 输入 | `battery_mv = 7000`（6000~8400，正常区间） |
| 操作步骤 | 1. `HAL_GPIO_SetPin_Expect(LED_RED, LOW)`<br>2. `HAL_GPIO_SetPin_Expect(LED_GREEN, HIGH)`<br>3. 调用 `led_set_by_voltage(7000)`<br>4. 校验 `led_get_state()` 返回值 |
| 预期结果 | 红灯 LOW、绿灯 HIGH；状态 = LED_STATE_GREEN |
| 实际结果 | 状态 = GREEN |
| 状态 | ✅ 通过 |
| 备注 | 验证正常区间 GPIO 操作和状态更新正确 |

### TC-014：过压区间 —— 红灯亮

| 项目 | 内容 |
|------|------|
| 用例编号 | TC-014 |
| 用例名称 | test_led_overvoltage |
| 测试方法 | 等价类划分（过压有效等价类） |
| 前置条件 | 桩状态已初始化 |
| 输入 | `battery_mv = 9000`（> 8400，过压区间） |
| 操作步骤 | 1. `HAL_GPIO_SetPin_Expect(LED_RED, HIGH)`<br>2. `HAL_GPIO_SetPin_Expect(LED_GREEN, LOW)`<br>3. 调用 `led_set_by_voltage(9000)`<br>4. 校验 `led_get_state()` 返回值 |
| 预期结果 | 红灯 HIGH、绿灯 LOW；状态 = LED_STATE_RED |
| 实际结果 | 状态 = RED |
| 状态 | ✅ 通过 |
| 备注 | 过压和低压 GPIO 操作相同但业务含义不同，分开测试确保两个分支都被覆盖 |

### TC-015：边界 —— 5999mV（低压上限）

| 项目 | 内容 |
|------|------|
| 用例编号 | TC-015 |
| 用例名称 | test_led_boundary_5999 |
| 测试方法 | 边界值分析 |
| 前置条件 | 桩状态已初始化 |
| 输入 | `battery_mv = 5999`（< 6000 的最后一个值） |
| 操作步骤 | 1. 预设红灯 HIGH、绿灯 LOW<br>2. 调用 `led_set_by_voltage(5999)`<br>3. 校验状态 |
| 预期结果 | 状态 = LED_STATE_RED |
| 实际结果 | RED |
| 状态 | ✅ 通过 |
| 备注 | 验证 < 6000 的最后一个值仍属于低压区间 |

### TC-016：边界 —— 6000mV（正常下限）

| 项目 | 内容 |
|------|------|
| 用例编号 | TC-016 |
| 用例名称 | test_led_boundary_6000 |
| 测试方法 | 边界值分析 |
| 前置条件 | 桩状态已初始化 |
| 输入 | `battery_mv = 6000`（≥ 6000 的第一个值） |
| 操作步骤 | 1. 预设红灯 LOW、绿灯 HIGH<br>2. 调用 `led_set_by_voltage(6000)`<br>3. 校验状态 |
| 预期结果 | 状态 = LED_STATE_GREEN |
| 实际结果 | GREEN |
| 状态 | ✅ 通过 |
| 备注 | **易错边界**：若条件写成 `<=` 而非 `<`，6000mV 会被误判为低压，此用例立刻失败 |

### TC-017：边界 —— 8400mV（正常上限）

| 项目 | 内容 |
|------|------|
| 用例编号 | TC-017 |
| 用例名称 | test_led_boundary_8400 |
| 测试方法 | 边界值分析 |
| 前置条件 | 桩状态已初始化 |
| 输入 | `battery_mv = 8400`（≤ 8400 的最后一个值） |
| 操作步骤 | 1. 预设红灯 LOW、绿灯 HIGH<br>2. 调用 `led_set_by_voltage(8400)`<br>3. 校验状态 |
| 预期结果 | 状态 = LED_STATE_GREEN |
| 实际结果 | GREEN |
| 状态 | ✅ 通过 |
| 备注 | 验证 ≤ 8400 的最后一个值仍属于正常区间 |

### TC-018：边界 —— 8401mV（过压下限）

| 项目 | 内容 |
|------|------|
| 用例编号 | TC-018 |
| 用例名称 | test_led_boundary_8401 |
| 测试方法 | 边界值分析 |
| 前置条件 | 桩状态已初始化 |
| 输入 | `battery_mv = 8401`（> 8400 的第一个值） |
| 操作步骤 | 1. 预设红灯 HIGH、绿灯 LOW<br>2. 调用 `led_set_by_voltage(8401)`<br>3. 校验状态 |
| 预期结果 | 状态 = LED_STATE_RED |
| 实际结果 | RED |
| 状态 | ✅ 通过 |
| 备注 | **易错边界**：若条件写成 `>=` 而非 `>`，8400mV 会被误判为过压，TC-017 会失败 |

### TC-019：状态切换 —— 从正常切到低压

| 项目 | 内容 |
|------|------|
| 用例编号 | TC-019 |
| 用例名称 | test_led_state_transition |
| 测试方法 | 状态切换测试 |
| 前置条件 | 桩状态已初始化 |
| 输入 | 第一次 7000mV（正常），第二次 5000mV（低压） |
| 操作步骤 | 1. 预设 4 次 GPIO 调用（正常 2 次 + 低压 2 次）<br>2. 调用 `led_set_by_voltage(7000)`，校验状态=GREEN<br>3. 调用 `led_set_by_voltage(5000)`，校验状态=RED |
| 预期结果 | 第一次后 GREEN，第二次后 RED；GPIO 按顺序调用 4 次 |
| 实际结果 | GREEN → RED，4 次 GPIO 调用全部匹配 |
| 状态 | ✅ 通过 |
| 备注 | 验证模块的"有状态"行为：current_state 静态变量在多次调用间正确更新；CMock 按顺序校验每次 GPIO 调用的 pin 和 level 参数 |

---

## 3. 用例与需求追溯矩阵

| 需求点 | 对应用例 | 说明 |
|--------|----------|------|
| ADC 原始值换算为电池电压 | TC-001, TC-002 | 验证换算公式正确性 |
| 电压上限钳位（≤8400mV） | TC-004, TC-005, TC-006 | 验证超压时钳位到最大值 |
| 非法输入防护（>4095 返回 0） | TC-007 | 验证超出硬件范围的输入被拦截 |
| 调用 HAL 读取 ADC | TC-008, TC-009 | 验证 HAL 调用次数和顺序正确 |
| HAL 错误容错 | TC-011 | 验证 HAL 返回错误码时返回 BATTERY_READ_ERROR |
| Ignore 模式支持 | TC-010 | 验证 CMock Ignore 插件可用 |
| 下边界处理（adc_raw=0） | TC-003 | 验证最小有效值处理正确 |
| LED 低压指示（<6000mV 红灯） | TC-012, TC-015 | 验证低压区间红灯亮 |
| LED 正常指示（6000~8400mV 绿灯） | TC-013, TC-016, TC-017 | 验证正常区间绿灯亮 |
| LED 过压指示（>8400mV 红灯） | TC-014, TC-018 | 验证过压区间红灯亮 |
| LED 状态切换 | TC-019 | 验证连续调用时状态正确更新 |
| GPIO 接口调用正确性 | TC-012 ~ TC-019 | 验证 HAL_GPIO_SetPin 调用次数、pin、level 参数 |

---

## 4. 查错能力验证记录

为证明测试用例能有效拦截缺陷，执行了"故意注入 bug"验证：

### 4.1 注入的缺陷

将 `battery.c` 中上钳位条件由 `>` 改为 `<`：

```c
// 正确代码
if (battery_mv > BATTERY_MAX_MV) { battery_mv = BATTERY_MAX_MV; }

// 注入缺陷（条件写反）
if (battery_mv < BATTERY_MAX_MV) { battery_mv = BATTERY_MAX_MV; }
```

### 4.2 缺陷影响分析

条件写反后，任何**小于** 8400 的值都会被错误钳位到 8400，而**大于** 8400 的超压值反而不被钳位。

### 4.3 测试拦截结果

| 用例编号 | 用例名称 | 注入缺陷后结果 | 失败原因 |
|----------|----------|---------------|----------|
| TC-001 | test_adc_to_mv_normal | ❌ FAIL | Expected 4950 Was 8400（正常值被误钳位） |
| TC-002 | test_adc_to_mv_below_max | ❌ FAIL | Expected 2415 Was 8400 |
| TC-003 | test_adc_to_mv_zero | ❌ FAIL | Expected 0 Was 8400（0 被误钳位） |
| TC-004 | test_adc_to_mv_full_scale_clamp | ❌ FAIL | Expected 8400 Was 9900（超压未钳位） |
| TC-005 | test_adc_to_mv_clamp_critical | ❌ FAIL | Expected 8400 Was 8403（临界超压未钳位） |
| TC-006 | test_adc_to_mv_overvoltage | ❌ FAIL | Expected 8400 Was 9669（过压未钳位） |
| TC-007 | test_adc_to_mv_invalid_input | ✅ PASS | 非法输入直接返回 0，不经过钳位逻辑 |
| TC-008 | test_read_battery_mv_success | ❌ FAIL | Expected 4950 Was 8400 |
| TC-009 | test_read_battery_mv_multi_calls | ❌ FAIL | Expected 2415 Was 8400 |
| TC-010 | test_read_battery_mv_ignore_mode | ❌ FAIL | Expected 4950 Was 8400 |
| TC-011 | test_read_battery_mv_hal_error | ✅ PASS | HAL 错误直接返回错误码，不经过换算 |

**汇总：11 个用例中 9 个失败，精准拦截了钳位条件写反的缺陷。** 仅有的 2 个通过用例（TC-007、TC-011）是因为它们的执行路径不经过被修改的钳位逻辑。

### 4.4 结论

本测试用例集具备有效的缺陷拦截能力。业务逻辑中的关键缺陷（如钳位条件写反）会在 PC 单元测试阶段被自动发现，无需等到硬件联调，实现了"测试左移"。

---

## 5. 执行环境与结果记录

| 项目 | 内容 |
|------|------|
| 执行日期 | 2026-09-05 |
| 操作系统 | Windows 10/11 |
| 编译器 | MinGW GCC（`C:\MinGW\bin\gcc.exe`） |
| 构建工具 | mingw32-make |
| 编译选项 | `-Wall -Wextra -std=c99` |
| 编译警告 | 0 |
| 用例总数 | 19（battery 11 + led 8） |
| 通过 | 19 |
| 失败 | 0 |
| 阻塞 | 0 |
| 通过率 | 100% |
| CI/CD | ✅ GitHub Actions 全流程通过（Makefile + CMake/CTest） |
| 查错能力验证 | ✅ 注入钳位条件写反缺陷后，9/11 battery 用例失败 |
